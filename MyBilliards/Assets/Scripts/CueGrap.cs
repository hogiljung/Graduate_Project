using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CueGrap : MonoBehaviour
{
    public bool IsGrap { get; set; }
    // Start is called before the first frame update
    void Start()
    {
        IsGrap = false;
    }
    
}
